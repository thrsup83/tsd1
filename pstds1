We have successfully updated several servers; however, some are experiencing PowerShell module errors that are causing the installer to hang. We have opened a new Change Request (CR) specifically to troubleshoot the PowerShell module on those affected servers

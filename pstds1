$RegistryPath = "HKLM:\SOFTWARE\Microsoft\Office\ClickToRun\Configuration"
$VersionValue = "VersionToReport"

if (Test-Path $RegistryPath) {
    (Get-ItemProperty -Path $RegistryPath).$VersionValue
} else {
    exit 1
}

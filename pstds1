$p = Get-CimInstance Win32_Processor
$s = if ($p.NumberOfLogicalProcessors -gt $p.NumberOfCores) { "Enabled" } else { "Disabled" }
return [PSCustomObject]@{
    HT_Status = $s
    Cores = "$($p.NumberOfCores)C/$($p.NumberOfLogicalProcessors)P"
}
